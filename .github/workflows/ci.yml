name: CI for llvm_ir

on:
  push:
    branches: [ llvm_ir ]
  pull_request:
    branches: [ llvm_ir ]

jobs:
  build-and-test:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t leo .
      - name: Build compiler 
        run: |
          docker run --rm -v "$(pwd)":/app leo ./docker-entrypoint.sh
      - name: Run ARM tests 
        run: |
          docker run --rm -v "$(pwd)":/app leo ./test.sh
      - name: Run LLVM tests
        run: |
          docker run --rm -v "$(pwd)":/app leo ./test_llvm.sh

